# Modules are python codes that are written in some other python files and can be imported to our current file
# and work with that codes
# pip is the standard package manager for python.It is used to install and manage packages or modules that aren't
# part of the Python Standard Library
# Modules can be imported using import command and the name of the module
# these modules can be used inside the program like given below

import docx
docx.

# use pip install package_name in CLI to install a particular package inside the site packages

